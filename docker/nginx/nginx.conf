worker_processes 1;
worker_rlimit_nofile 500000;

events {
    use epoll;
    worker_connections 2048;
}

stream {
    upstream api {
        server api1:8080;
        server api2:8081;
    }

    server {
        listen 9999;
        proxy_pass api;
    }
}
