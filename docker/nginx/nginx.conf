worker_processes auto;
worker_rlimit_nofile 65536;

events {
    use epoll;
    multi_accept on;
    worker_connections 65535;
}

stream {
    upstream api {
        server api1:8080;
        server api2:8081;
    }

    server {
        listen 9999;
        proxy_pass api;
    }
}
